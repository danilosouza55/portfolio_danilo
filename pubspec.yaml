name: portfolio_danilo
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  url_launcher: ^6.2.4
  layout: ^1.0.5
  responsive_builder: ^0.7.0
  flutter_native_splash: ^2.3.13

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true

  fonts:
    - family: LandingPageIcons
      fonts:
        - asset: assets/fonts/LandingPageIcons.ttf
  